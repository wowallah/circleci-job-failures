version: 2.1

jobs:
  my_job:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: return 1
      - run: echo "Failed"
          when: on_fail
            
workflows:
  my_workflow:
    jobs:
      - my_job